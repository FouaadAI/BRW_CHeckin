{
  "name": "skill-optimize",
  "description": "Automatically selects optimal skills and MCP servers per context, tracks usage, and prunes zombies.",
  "hooks": {
    "sessionStart": {
      "command": "python .github/scripts/context_loader.py '${CWD_CONTEXT}' 15",
      "description": "Generate active skill manifest based on current working directory context"
    },
    "postToolUse": {
      "command": "python .github/scripts/skill_tracker.py log '${SKILL_NAME}' '${CONTEXT}' '${FILE_TYPE}' '${TRIGGERED_BY}' '${SESSION_ID}'",
      "description": "Log skill invocation to usage database"
    },
    "sessionEnd": {
      "command": "python .github/scripts/skill_ranker.py generate && python .github/scripts/mcp_optimizer.py report",
      "description": "Update tier list and report MCP stats at session end"
    }
  }
}
